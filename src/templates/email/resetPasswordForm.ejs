<!DOCTYPE html>
<html>
<head>
    <title>Reset Password</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f9f9f9; text-align: center; padding-top: 100px; }
        .form-container { background: #fff; display: inline-block; padding: 40px 60px; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.1);}
        input { margin: 10px 0; padding: 10px; width: 100%; border-radius: 4px; border: 1px solid #ccc; }
        button { padding: 10px 20px; border: none; border-radius: 4px; background: #4CAF50; color: #fff; font-size: 16px; }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Reset Your Password</h1>
        <form method="POST" action="<%= postUrl %>">
            <input type="password" name="password" placeholder="New Password" required minlength="6">
            <button type="submit">Reset Password</button>
        </form>
    </div>
</body>
</html>